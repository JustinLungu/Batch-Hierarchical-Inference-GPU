# ExPECA public-IP remote experiment settings.
#
# Edit these after the notebook creates the remote containers.
# The edge-server and edge-device public IPs are printed by
# notebooks/ExPECA_HI_setup_Public_IP.ipynb.

DEVICE=cpu
EDGE_SERVER_IP=130.237.11.114
EDGE_DEVICE_IP=130.237.11.115
EDGE_SERVER_PORT=8001
EDGE_DEVICE_PORT=8000

# First remote CPU baseline. Increase these later for the batch-size sweep.
BATCH_SIZE=4
CONTROLLER_BATCH_SIZE=4
FLUSH_FINAL_BATCH=true
